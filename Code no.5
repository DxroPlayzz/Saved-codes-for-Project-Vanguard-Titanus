# install git if you don't have it
git clone https://github.com/yourusername/invictus-hydra-mini.git
cd invictus-hydra-mini
# add files, then:
git add .
git commit -m "Initial commit: weapon controller code + wiring diagram"
git push origin main
